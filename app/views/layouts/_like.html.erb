<div class="like" id="like-link-<%= post.id %>" hidden>
  <% if current_user.likes.find_by(post_id: post.id) %>
    <%= link_to unlike_path(post.id), method: :delete, remote: true do %>
        <div class = "iine__button">
          <i class="fas fa-heart"></i>
          <%= post.likes.count %>
        </div>
    <% end %>
  <% else %>
    <%= link_to like_path(post.id), method: :post, remote: true do %>
        <div class = "iine__button">
          <i class="far fa-heart">
          </i><%= post.likes.count %>
        </div>
    <% end %>
  <% end %>
</div>

<script>
var user = <%= { id: current_user.try(:id) }.to_json.html_safe %>;
var drink = <%= { id: drink.try(:id) }.to_json.html_safe %>;
</script>