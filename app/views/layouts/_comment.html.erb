<div class="comment">
  <h4>コメント一覧</h4>
    <% if @comments.present? %>
      <% @comments.each do |comment| %>
      <div class="comment-box">
        <div class="comment-header">
          <div class="comment-header-left">
            <%= link_to image_tag(comment.user.user_image.thumb.url,id:"commenter-icon"),user_path(comment.user)%>
            <%= comment.user.name%>
          </div>
          <div class="comment-header-right">
              <% if current_user == comment.user%>
              <%= button_to "削除", post_comments_path(comment.post_id,comment.id),  { method: :delete, id:"comment-delete-btn"} %>
              <%end%>
              <%= l comment.created_at%>
          </div>
        </div>
        <p id="comment-content"><%= comment.content %></p>
      <%end%>
      </div>
    <% else %>
      <p>
      この投稿にはコメントがありません
      </p>
    <%end%>
</div>    