<%if logged_in?%>
  <%= form_with(model: [@post, @comment], local: true) do |f| %>
  <%= f.text_area :content,placeholder: "コメントを入力", rows: "3", id:"comment-input-field"%>
  <%= f.submit "コメントする", id:"comment-button"%>
  <%end%>
<% end %>